# WikiLens — Remaining TODO

> Generated by analyzing the backend and frontend source against `plan.md`.
> Items already implemented are **not** listed here.

---

## 🔴 Backend — Critical / Core Missing

### Wikitext Markup Stripper
The XML parser feeds raw Wikitext directly into the tokenizer with no cleanup.
This degrades index quality significantly.

- [ ] Strip templates: `{{Infobox person | ...}}`
- [ ] Convert internal wiki links: `[[Alan Turing|Turing]]` → keep display text
- [ ] Remove external links: `[http://example.com Example]` → keep `Example`
- [ ] Remove heading markers: `== Early life ==` → keep `Early life`
- [ ] Remove bold/italic markers: `'''bold'''` and `''italic''`
- [ ] Remove reference tags: `<ref>...</ref>` (including content)
- [ ] Remove HTML comments: `<!-- ... -->`
- [ ] Remove tables: `{| ... |}` (skip for v1 if too complex)
- [ ] Create `src/wikitext_stripper.cpp` + `include/wikitext_stripper.h`
- [ ] Wire it into the ingestion pipeline in `main.cpp` between the XML parser callback and the tokenizer

### Link Graph Extraction & PageRank
- [ ] While stripping Wikitext, extract every `[[Internal Link]]` and record edge `(source_title → target_title)`
- [ ] Save link graph as a plain-text edge list file (e.g., `data/link_graph.txt`)
- [ ] Implement offline PageRank computation (iterative power method, ~20–50 iterations)
- [ ] Create `src/pagerank.cpp` + `include/pagerank.h`
- [ ] Store per-document PageRank scores and make them accessible to the query engine
- [ ] Combine BM25 score + PageRank into a final blended score in `QueryEngine::search()`

### Index Persistence (Disk Serialization)
Currently the index is rebuilt from scratch every startup, which is very slow at scale.

- [ ] Implement `InvertedIndex::saveToDisk(const std::string& path)` in `inverted_index.cpp`
- [ ] Implement `InvertedIndex::loadFromDisk(const std::string& path)` in `inverted_index.cpp`
- [ ] Use a binary format: term dictionary header + posting list data
- [ ] On startup in `main.cpp`: check if a saved index exists → load it; otherwise build and save
- [ ] Also persist `DocumentStore` to disk and reload it

### Snippet / Excerpt Extraction
- [ ] Store a short plain-text excerpt (first ~300 chars of stripped body) per document in `DocumentStore`
- [ ] Update `Document` struct in `include/document_store.h` to include `std::string snippet`
- [ ] Return `snippet` field in the `/search` JSON response in `main.cpp`

### BM25 `avgDocLength()` Implementation
- [ ] Implement `InvertedIndex::avgDocLength()` in `inverted_index.cpp` — currently declared but likely a stub
- [ ] Compute it as `total_token_count / total_docs_` and cache it after index build

### Wikipedia URL Construction
- [ ] Construct proper Wikipedia URLs in the ingestion pipeline: `https://en.wikipedia.org/wiki/{URL-encoded title}`
- [ ] Store this URL in `DocumentStore` and return it in the search API response

---

## 🟡 Backend — Quality & Robustness

### bz2 Decompression Support
Currently only uncompressed XML dumps are supported (`.xml.1` file in repo root).

- [ ] Add `libbz2` dependency to `CMakeLists.txt`
- [ ] Implement on-the-fly bz2 decompression in `wiki_parser.cpp` so compressed `.xml.bz2` dumps can be streamed directly without pre-extraction

### Query API Improvements
- [ ] Return `url` and `snippet` fields in the `/search` JSON response (currently only title + score)
- [ ] Add a `?limit=` query parameter to control result count (default 10)
- [ ] Add proper CORS headers for local development (currently frontend at port 5173, backend at 8080)
- [ ] Add a `/stats` endpoint returning total docs indexed, avg doc length, index term count

### LRU Query Cache
- [ ] Implement `include/lru_cache.h` (template LRU cache using `std::list` + `std::unordered_map`)
- [ ] Add an LRU cache in `main.cpp` for recent query results (cache size ~1000 queries)

### Error Handling
- [ ] Handle missing/wrong XML dump path gracefully with a clear error message
- [ ] Return proper HTTP 400 for empty or missing `q` parameter in `/search`
- [ ] Add input length limit on query string to prevent abuse

### Logging & Progress
- [ ] Add elapsed time logging during indexing (currently only doc count is printed)
- [ ] Log memory usage at end of indexing phase

---

## 🟡 Frontend — Missing Features

### Display Snippet & URL
- [ ] Update `Result` type in `App.tsx` to include `snippet: string` and `url: string`
- [ ] Display the snippet text below the title in each result card
- [ ] Make the result card title a clickable link to the Wikipedia URL (open in new tab)

### Pagination
- [ ] Add "Load more" button or page navigation below results
- [ ] Pass `page` or `offset` parameter to the backend API

### Autocomplete / Search Suggestions
- [ ] Add a dropdown suggestion list below the search input as user types
- [ ] Debounce input (300ms) before firing suggestion requests
- [ ] Add a `/suggest` endpoint on the backend using a trie or prefix scan of the term dictionary

### Loading & Error States
- [ ] Show a proper loading spinner during search (not just hidden state)
- [ ] Show a user-friendly error message if the backend is unreachable
- [ ] Disable search button while a request is in flight

### UI Polish
- [ ] Update `<title>` in `frontend/index.html` from `"frontend"` to `"WikiLens"`
- [ ] Add a favicon (replace default Vite SVG)
- [ ] Make layout responsive for mobile viewports
- [ ] Add keyboard shortcut: press `/` to focus search input

---

## 🔵 Testing — Nothing Exists Yet

The plan calls for a full test suite; none exists currently.

- [ ] Set up GoogleTest via CMake `FetchContent` in `CMakeLists.txt`
- [ ] `tests/test_tokenizer.cpp` — test lowercase, punctuation stripping, stop word removal, stemming
- [ ] `tests/test_inverted_index.cpp` — test `addDocument`, `lookup`, `docLength`, `avgDocLength`
- [ ] `tests/test_bm25.cpp` — test BM25 scores against known expected values
- [ ] `tests/test_query_engine.cpp` — test single-term, multi-term, and empty queries
- [ ] `tests/test_wikitext_stripper.cpp` — test each stripping rule with fixture strings

---

## 🔵 Scale & Polish (Phase 6)

- [ ] Benchmark query latency with the full Simple English dump (~220K docs)
- [ ] Profile and optimize the tokenizer (hot path during indexing)
- [ ] Implement index partitioning design doc (even if not building it yet)
- [ ] Write `README.md` for the backend with build instructions, API docs, and architecture notes
- [ ] Update the top-level `README.md` with project overview, demo screenshot, and how to run

---

## ✅ Already Done (for reference)

| Component | Status |
|-----------|--------|
| CMake project structure | ✅ |
| Tokenizer + Porter Stemmer | ✅ |
| Inverted Index (in-memory) | ✅ |
| BM25 scorer | ✅ |
| Query Engine | ✅ |
| Document Store | ✅ |
| SAX XML parser (libexpat) | ✅ |
| Wikipedia ingestion pipeline wired in main | ✅ |
| Crow HTTP server on port 8080 | ✅ |
| `/search` JSON API endpoint | ✅ |
| React + TypeScript + Vite frontend | ✅ |
| Search UI with glassmorphism + framer-motion | ✅ |
| Axios integration to backend | ✅ |
| Display title + BM25 score per result | ✅ |
